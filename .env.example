ANTHROPIC_API_KEY=sk-ant-...

# Daily outfit emails use the Gmail API with OAuth2 — no password needed.
# Copy credentials.json from your calendar-agent/credentials/ folder into
# this project's credentials/ folder, then run daily_outfit.py once to
# sign in through your browser. The token is saved automatically after that.
